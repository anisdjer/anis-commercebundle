{% extends '::base.html.twig' %}

{% block body -%}
    <h1>LigneFacture list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Quantity</th>
                <th>Unityprice</th>
                <th>Discount</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('lignefacture_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.quantity }}</td>
                <td>{{ entity.unityPrice }}</td>
                <td>{{ entity.discount }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('lignefacture_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('lignefacture_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('lignefacture_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
